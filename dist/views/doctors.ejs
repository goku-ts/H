<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div class="w-full h-52 bg-white border border-gray-200">
        <!-- Header -->
        <div class="flex items-center justify-between p-5">
            <h1 class="text-xl font-bold text-blue-400">Doctors</h1>
        </div>
        <table class="w-full h-52 bg-white border border-gray-200">
            <thead>
                <tr class="bg-gray-100">
                    <th class="py-2 px-4 border-b border-gray-200 text-left text-sm font-semibold text-gray-700">ID</th>
                    <th class="py-2 px-4 border-b border-gray-200 text-left text-sm font-semibold text-gray-700">Name
                    </th>
                    <th class="py-2 px-4 border-b border-gray-200 text-left text-sm font-semibold text-gray-700">
                        Specialization</th>
                </tr>
            </thead>
            <tbody>

                <% doctors.forEach(doctor=> { %>
                    <tr class="hover:bg-gray-50 cursor-pointer"
                        onclick="window.location.href='/doctor/<%= doctor.id %>'">
                        <td class="py-2 px-4 border-b border-gray-200 text-sm text-gray-600">
                            <%= doctor.id %>
                        </td>
                        <td class="py-2 px-4 border-b border-gray-200 text-sm text-gray-600">
                            <%= doctor.name %>
                        </td>
                        <td class="py-2 px-4 border-b border-gray-200 text-sm text-gray-600">
                            <%= doctor.specialization %>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>
</body>

</html>